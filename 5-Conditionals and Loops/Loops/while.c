#include <stdio.h>

void main(int argc, char **argv[])
{
    int count = 1;

    // executes statements repeatedly while an expression is true
    while (count < 5 ){
        printf("count = %d \n", count);
        count++;

    }
}
